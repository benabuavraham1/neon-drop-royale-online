{
  "name": "neon-drop-royale-multiplayer",
  "version": "6.0.0",
  "private": true,
  "description": "Original top-down multiplayer battle royale prototype with a larger named map, mixed inventory, smarter bots and private lobbies",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/game.js",
    "test": "node test/game.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.3"
  }
}
